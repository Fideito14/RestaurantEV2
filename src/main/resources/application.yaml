server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: menu-service
              uri: http://menu-service:8081
              predicates:
                - Path=/api/platos/**

            - id: pedidos-service
              uri: http://pedidos-service:8082
              predicates:
                - Path=/api/pedidos/**